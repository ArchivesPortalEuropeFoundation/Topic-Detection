name: Docker build and test

on: [push,pull_request]

jobs:
  build:
   runs-on: ubuntu-latest
  
   steps:
     - uses: actions/checkout@v2
     - name: Build Docker image 
       run: docker build --tag ape .
     - name: Run tests inside the container
       run: docker run ape pytest